<Page x:Class="SEKTY_ON.PeticionesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PeticionesPage">

    <Grid Background="White">
        <StackPanel Background="#660636" VerticalAlignment="Top" Height="80">
            <Label Content="Peticiones de Usuarios" Foreground="White" FontSize="40"
                   FontFamily="/Desing/Fonts/#Kikuta Thin" Margin="10"/>
        </StackPanel>

        <ComboBox x:Name="cbFiltro" HorizontalAlignment="Right" Margin="0,23,45,0"
                  VerticalAlignment="Top" Width="189" Height="26"
                  SelectionChanged="FiltroEstado_SelectionChanged">
            <ComboBoxItem Content="Todas" IsSelected="True"/>
            <ComboBoxItem Content="Pendientes"/>
            <ComboBoxItem Content="Aprobadas"/>
            <ComboBoxItem Content="Rechazadas"/>
        </ComboBox>

        <ListView Margin="10,85,10,40" x:Name="lvPeticiones">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#D9D9D9" BorderThickness="1" CornerRadius="8"
                            Padding="10" Margin="0,0,0,10" Width="733">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Ellipse Width="60" Height="60">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="/Desing/Iconos/usuarioPerfil.png" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>

                            <StackPanel Grid.Column="1" Margin="15,0,10,0">
                                <TextBlock Text="{Binding UsuarioId, StringFormat='Usuario ID: {0}'}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding LaboratorioId, StringFormat='Laboratorio ID: {0}'}" Margin="0,2,0,5"/>
                                <TextBlock Text="{Binding FechaCreacion, StringFormat='Solicitado el: {0:dd/MM/yyyy HH:mm}'}" FontSize="11" Foreground="Gray"/>
                                <TextBlock Text="{Binding Notas, StringFormat='Notas: {0}'}" FontSize="12" FontStyle="Italic" Margin="0,5,0,0" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Aceptar ✔" Background="Green" Foreground="White" Width="100" Margin="5" Height="30" Click="BtnAceptar_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Estatus}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="Rechazar ✖" Background="Red" Foreground="White" Width="100" Margin="5" Height="30" Click="BtnRechazar_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Estatus}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="txtMensajeOcupado" 
                   Text="No puedes aceptar la petición porque el laboratorio está ocupado o en mantenimiento"
                   Foreground="Red" FontSize="12" Margin="20,0,0,10"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom" Visibility="Collapsed"/>
    </Grid>
</Page>